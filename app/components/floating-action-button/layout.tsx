import { DocsShell } from "@/components/docs/docs-shell";

export default function FloatingActionButtonDocsLayout({ children }: { children: React.ReactNode }) {
  return <DocsShell>{children}</DocsShell>;
}

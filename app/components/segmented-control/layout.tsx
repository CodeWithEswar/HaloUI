import { DocsShell } from "@/components/docs/docs-shell";

export default function SegmentedControlDocsLayout({ children }: { children: React.ReactNode }) {
  return <DocsShell>{children}</DocsShell>;
}
